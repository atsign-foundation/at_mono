name: c_dart_interop_tests
publish_to: none

environment: 
  sdk: "^3.0.0"

dependencies:
  ffi: ^2.1.0
  at_chops:
    path: ../../modules/dart/at_libraries/packages/at_chops

dev_dependencies:
  test: ^1.25.0
